# Dipendenze runtime comuni a tutti gli ambienti.
# Pin a minimi compatibili (>=) per avere wheel precompilate anche su Python recenti
# (es. 3.14). Per build riproducibili si potrà passare a pin esatti / lock file.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0.36
pydantic>=2.11
pydantic-settings>=2.6
alembic>=1.13
